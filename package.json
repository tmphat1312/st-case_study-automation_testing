{
  "name": "st-case_study-automation_testing",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "clean": "rimraf test-results playwright playwright-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "dotenv": "^16.4.7",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "csv-parse": "^5.6.0"
  }
}
